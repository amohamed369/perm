---
phase: 04-backend-services-cleanup
plan: 01
type: execute
---

<objective>
Clean calendar services by removing emoji logging markers and aligning with Phase 2 logging standards.

Purpose: Calendar services (calendar_service.py, calendar_integration.py) use emoji markers in log statements which is inconsistent with Phase 2 logging patterns (plain text, structured).
Output: Calendar services with clean, consistent logging matching main.py/orchestrator patterns.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-logging-infrastructure/02-01-SUMMARY.md

**Prior decisions affecting this phase:**
- Phase 2: Log format is `%(asctime)s - %(name)s - %(levelname)s - %(message)s` (standard, parseable)
- Phase 2: Removed [ORCH-v2] and [ORCH] prefixes - logger name provides context
- Phase 2: Noisy loggers set to WARNING level
- Phase 3: Plain text logs, no special characters

**Pattern to follow (from main.py):**
```python
logger = logging.getLogger(__name__)
logger.info("Starting backend server")  # Plain text, no emoji
logger.debug("Debug message here")
```

**Files to clean:**
@backend/app/services/calendar_service.py
@backend/app/services/calendar_integration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean calendar_service.py emoji logging</name>
  <files>backend/app/services/calendar_service.py</files>
  <action>
    1. Find all logger.info/debug/warning/error calls with emoji markers
    2. Remove emoji characters from log messages
    3. Keep the informative text content
    4. Ensure logger = logging.getLogger(__name__) pattern is used

    Example transformation:
    - BEFORE: logger.info(f"🎯 CalendarService.create_event called...")
    - AFTER: logger.info(f"CalendarService.create_event called...")

    Do NOT change:
    - Function logic or behavior
    - Exception handling
    - Return values
  </action>
  <verify>grep -E "[🎯🔑✅❌🏗️📋🚀💥🔧⚠️📅🗓️]" backend/app/services/calendar_service.py returns no matches</verify>
  <done>No emoji characters in any log statements in calendar_service.py</done>
</task>

<task type="auto">
  <name>Task 2: Clean calendar_integration.py emoji logging</name>
  <files>backend/app/services/calendar_integration.py</files>
  <action>
    1. Find all logger.info/debug/warning/error calls with emoji markers
    2. Remove emoji characters from log messages
    3. Keep the informative text content
    4. Verify logger = logging.getLogger(__name__) pattern is used

    Same transformation pattern as Task 1.

    Do NOT change:
    - Function logic or behavior
    - Exception handling
    - Google Calendar API integration code
  </action>
  <verify>grep -E "[🎯🔑✅❌🏗️📋🚀💥🔧⚠️📅🗓️]" backend/app/services/calendar_integration.py returns no matches</verify>
  <done>No emoji characters in any log statements in calendar_integration.py</done>
</task>

<task type="auto">
  <name>Task 3: Verify calendar services consistency</name>
  <files>backend/app/services/calendar_service.py, backend/app/services/calendar_integration.py</files>
  <action>
    1. Run tests to ensure no regressions: pytest backend/tests/ -v
    2. Verify both files use consistent logging pattern:
       - logger = logging.getLogger(__name__) at module level
       - No emoji in any string literals
       - Log levels appropriate (debug for verbose, info for operations, warning/error for problems)
  </action>
  <verify>pytest backend/tests/ -v passes (227 tests expected)</verify>
  <done>All tests pass, calendar services follow Phase 2 logging standards</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] grep for emoji characters in both files returns empty
- [ ] Both files have logger = logging.getLogger(__name__) pattern
- [ ] pytest backend/tests/ -v passes all tests
- [ ] No changes to function behavior or API contracts
</verification>

<success_criteria>
- All emoji characters removed from calendar_service.py and calendar_integration.py
- Logging pattern matches Phase 2 standard (plain text, structured)
- All 227 tests pass
- No functional changes to calendar integration
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-services-cleanup/04-01-SUMMARY.md`
</output>
