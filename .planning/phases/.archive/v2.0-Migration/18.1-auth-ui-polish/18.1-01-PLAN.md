---
phase: 18.1-auth-ui-polish
plan: 01
type: execute
---

<objective>
Polish auth page structure and form aesthetics to match v1 brutalist design.

Purpose: Auth pages should feel like part of the PERM Tracker app with consistent header/footer chrome and bold neobrutalist form styling.
Output: Updated auth layout with header/footer, refined form containers with GlassPanel and hard shadows.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/FRONTEND_DESIGN_SKILL.md
@.planning/phases/18.1-auth-ui-polish/18.1-CONTEXT.md
@.planning/V2_DESIGN_TOKENS.json
@v2/docs/DESIGN_SYSTEM.md

**Design References (MUST review before implementation):**
@.planning/phases/17-design-system/design3
@.planning/phases/17-design-system/design4
@.planning/phases/17-design-system/design5

**Current Auth Files:**
@v2/src/app/(auth)/layout.tsx
@v2/src/app/(auth)/login/page.tsx
@v2/src/app/(auth)/signup/page.tsx
@v2/src/app/(auth)/reset-password/page.tsx

**Prior context:**
- Phase 17 established neobrutalist design system with hard shadows, black borders, Space Grotesk typography
- Phase 17.1 refined components including GlassPanel for frosted containers
- Phase 18 completed auth logic (Convex Auth with email/password and Google OAuth)
- This phase is purely visual/UX polish - no auth logic changes

**Design system patterns to apply:**
- Header: Consistent site framing with brand mark, minimal navigation
- Footer: Monospace text, hazard stripes (optional), copyright
- Cards/Forms: 2px black borders, hard shadows (4px/8px), zero radius
- Typography: Space Grotesk for headings, Inter for body, JetBrains Mono for labels
- Effects: Grain overlay, bg-dots pattern, hover lift on interactive elements
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance auth layout with header/footer chrome</name>
  <files>v2/src/app/(auth)/layout.tsx</files>
  <exploration>
    Read `.planning/FRONTEND_DESIGN_SKILL.md` for design patterns.
    Review design3, design4, design5 for header/footer examples.
    Check v2/docs/DESIGN_SYSTEM.md for existing utility classes.
  </exploration>
  <action>
    Update auth layout to include:

    1. **Header section** (top of page):
       - Left: PERM Tracker brand (Space Grotesk, uppercase, with accent color on "TRACKER")
       - Right: Subtle status/phase indicator (mono font, small text)
       - Border-bottom 2px black for separation
       - Match the header pattern from design3/design4/design5

    2. **Footer section** (bottom of page):
       - Left: Version/product info (mono, small, muted)
       - Right: Copyright text
       - Border-top 2px black
       - Optional: hazard-stripes divider above footer (subtle)
       - Use absolute positioning at bottom, ensure content doesn't overlap

    3. **Main content area**:
       - Keep grain-overlay and bg-dots
       - Center form vertically between header/footer
       - Maintain max-w-md constraint

    4. **Mobile responsive**:
       - Header/footer stack on mobile if needed
       - Maintain readability at all breakpoints

    Do NOT change auth logic or form structure - layout only.
  </action>
  <verify>
    - npm run dev starts without errors
    - Navigate to /login - header and footer visible
    - Navigate to /signup - same header/footer
    - Navigate to /reset-password - same header/footer
    - No TypeScript errors
  </verify>
  <done>
    Auth layout has consistent header/footer chrome matching v1 brutalist aesthetic.
    All three auth pages share the same visual framing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply brutalist form styling to auth pages</name>
  <files>
    v2/src/app/(auth)/login/page.tsx,
    v2/src/app/(auth)/signup/page.tsx,
    v2/src/app/(auth)/reset-password/page.tsx
  </files>
  <exploration>
    Check existing Card, Input, Button component styling in v2/src/components/ui/.
    Review GlassPanel component if available.
    Look at design examples for form input styling patterns.
  </exploration>
  <action>
    Enhance form styling on all three auth pages:

    1. **Form container** (Card):
       - Consider using GlassPanel for frosted glass effect if available
       - Ensure 2px black border and hard shadow are applied
       - Add subtle hover lift effect (translate-y + shadow increase)

    2. **Input fields**:
       - Verify hard shadow styling (shadow-hard-sm class)
       - Add focus states with Forest Green ring
       - Labels should be uppercase, mono font, bold
       - Consider adding subtle hover lift on inputs

    3. **Buttons**:
       - Primary button: Forest Green bg, white text, hard shadow
       - Active state: Press down (translate + no shadow)
       - Outline button (Google): Black border, hover fills black
       - Loading state styling (will be enhanced in next plan)

    4. **Divider** ("Or continue with"):
       - Use thick border (2px)
       - Background pill for text should match card background
       - Uppercase, mono font, tracking-wide

    5. **Links** (Forgot password, Sign up):
       - Underline on hover
       - Bold weight
       - Forest Green on hover

    6. **Error states** (if visible):
       - Red border on inputs
       - Red text for error messages
       - Bold, uppercase for emphasis

    Apply changes consistently across all three pages.
    Do NOT change form logic or validation - styling only.
  </action>
  <verify>
    - npm run build succeeds without TypeScript errors
    - Login page form has brutalist styling
    - Signup page form matches login styling
    - Reset password page form matches login styling
    - Hover effects work on inputs and buttons
    - Focus states show Forest Green ring
  </verify>
  <done>
    All auth forms have consistent brutalist styling:
    - Hard shadows on containers and inputs
    - Proper typography hierarchy (Space Grotesk headings, mono labels)
    - Interactive hover/focus states
    - Visual consistency across all three pages
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Header/footer visible on all auth pages
- [ ] Forms have hard shadow styling
- [ ] Hover effects work on interactive elements
- [ ] Mobile responsive (check at 375px width)
- [ ] No visual glitches or layout shifts
</verification>

<success_criteria>
- Auth pages have consistent header/footer chrome
- Forms use brutalist styling (hard shadows, black borders, zero radius)
- Typography follows design system (Space Grotesk headings, Inter body, mono labels)
- Interactive elements have proper hover/focus/active states
- Mobile responsive layout
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-auth-ui-polish/18.1-01-SUMMARY.md`:

# Phase 18.1 Plan 01: Auth Structure & Form Styling Summary

**[One-liner describing what shipped]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- `path/to/file.tsx` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 18.1-02-PLAN.md (Loading states and human verification)
</output>
